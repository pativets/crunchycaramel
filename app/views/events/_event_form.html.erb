<%= form_for @event do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <table class="event_form">
    <tbody>
      <tr>
        <th>Type</th>
        <td><%= f.select :category, Event::EVENT_TYPE %></td>
      </tr>
      <tr>
        <th>Start</th>
        <td>
          <%= f.text_field :start_date, :size => 10 %>
          <%= f.time_select :start_time, :minute_step => 15 %>
        </td>
      </tr>
      <tr>
        <th>End</th>
        <td>
          <%= f.text_field :end_date, :size => 10   %>
          <%= f.time_select :end_time, :minute_step => 15 %>
        </td>
      </tr>
      <tr>
        <th>Title</th>
        <td><%= f.text_field :title, :size => "57" %></td>
      </tr>
      <tr>
        <th>Location</th>
        <td><%= f.text_field :location, :size => "57" %></td>
      </tr>
      <tr id="price_field" class="hidden_elem">
        <th>Price</th>
        <td><%= f.text_field :price, :size => "57" %> CAD</td>
      </tr>
      <tr>
        <th>Description</th>
        <td><%= f.text_area :description %></td>
      </tr>
      <tr>
        <td></td>
        <td><%= f.submit "Create event!" %></td>
      </tr>
    </tbody>
  </table>
<% end %>
<%= javascript_tag "price_toggle()" %>